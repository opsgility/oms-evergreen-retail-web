@model IEnumerable<Contoso.DAL.Models.Employee>

@{
    ViewBag.Title = "Employees";
}

<h1>@ViewBag.Title</h1>

<p>
    @Html.ActionLink("Create New", "Create", "Employees", null, new { @class = "glossyBox createNew", id = "create-new-employee-button" })
</p>
<table class="dataTable">
    <tr>
        <th>Actions</th>
        <th>First Name</th>
        <th>Last Name</th>
        <th>Address</th>
        <th>Phone</th>
        <th>Service Areas</th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }, new { @class = "edit-employee-button" }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }, new { @class = "employee-details-button" }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID }, new { @class = "delete-employee-button" })
        </td>
        <td>
            @item.FirstName
        </td>
        <td>
            @item.LastName
        </td>
        <td>
            @Html.DisplayTextFor(_ => item.Address).ToString()
        </td>
        <td>
            @Html.DisplayTextFor(_ => item.Phone).ToString()
        </td>
        <td>
            @item.ServiceAreas
        </td>
    </tr>
}

</table>


